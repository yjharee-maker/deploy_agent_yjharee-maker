1. Create a parent dir called attendance_tracker_v1, 2-sub dir of Helpers and reports and 4 files with data; attendance_checker.py, assets.csv, config.json, reports.log. attendance_checker.py in project root dir, assets.csv and config.json in Helpers dir and reports.log in reports dir.
2. Gives a choice to update conditions for warning and failing.
3. Activates a function when Ctrl + c is done that archives the incomplete work and deletes the latter.
4. Catch a signal to terminate a process.
5. Verify if python is present.
6. Verify if required application dir is present.

